# runtime
mitm-tier.log
mitm-tier.pid
calls.log
*.mitm
ca-bundle-mitm.pem
__pycache__/
*.pyc

# debug captures — CONTAIN LIVE OAuth TOKENS / Authorization headers, never commit
capture-api/
capture-oauth/
capture-*.py
capture-*.log
capture-*.pid
